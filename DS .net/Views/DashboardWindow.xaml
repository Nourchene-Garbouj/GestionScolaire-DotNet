<Window x:Class="DS_.net.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tableau de Bord" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2e">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0" Text="📊 Tableau de Bord"
                   FontSize="24" FontWeight="Bold"
                   Foreground="White" Margin="0,0,0,20"/>

        <!-- Cartes statistiques -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Carte 1 : Total étudiants -->
            <Border Grid.Column="0" Background="#7c3aed" 
                    CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="👨‍🎓 Total Étudiants" 
                               Foreground="White" FontSize="14"/>
                    <TextBlock x:Name="TxtTotalEtudiants" 
                               Foreground="White" FontSize="36" 
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Carte 2 : Moyenne générale -->
            <Border Grid.Column="1" Background="#22c55e"
                    CornerRadius="10" Padding="20" Margin="5,0">
                <StackPanel>
                    <TextBlock Text="📈 Moyenne Générale"
                               Foreground="White" FontSize="14"/>
                    <TextBlock x:Name="TxtMoyenneGenerale"
                               Foreground="White" FontSize="36"
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Carte 3 : Taux de réussite -->
            <Border Grid.Column="2" Background="#f59e0b"
                    CornerRadius="10" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="✅ Taux de Réussite"
                               Foreground="White" FontSize="14"/>
                    <TextBlock x:Name="TxtTauxReussite"
                               Foreground="White" FontSize="36"
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Liste des moyennes par étudiant -->
        <Border Grid.Row="2" Background="#2e2e3e" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="📋 Moyennes par Étudiant"
                           Foreground="White" FontSize="16"
                           FontWeight="Bold" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" x:Name="DgMoyennes"
                          Background="#2e2e3e" Foreground="White"
                          BorderBrush="#555" RowBackground="#2e2e3e"
                          AlternatingRowBackground="#252535"
                          GridLinesVisibility="Horizontal"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#7c3aed"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                        <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                        <DataGridTextColumn Header="Classe" Binding="{Binding Classe}" Width="*"/>
                        <DataGridTextColumn Header="Moyenne" Binding="{Binding Moyenne, StringFormat=F2}" Width="*"/>
                        <DataGridTextColumn Header="Résultat" Binding="{Binding Resultat}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>